apiVersion: 1

providers:
  - name: "iot_dashboards"
    orgId: 1
    folder: "IoT Dashboards"       # Nom du dossier dans l’UI Grafana
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 10

    options:
      path: /etc/grafana/dashboards
